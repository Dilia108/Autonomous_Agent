# LLM + agent framework
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0

# RAG (full agent only — not needed for prototype.py)
langchain-chroma>=0.1.4
chromadb>=0.5.0

# Search tool
tavily-python>=0.3.0

# Server (full agent only — not needed for prototype.py)
flask>=3.0.0

# Environment variables
python-dotenv>=1.0.0
